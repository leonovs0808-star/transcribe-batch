---
name: transcribe-batch
description: |
  Дословная расшифровка аудио/видео в текст (Groq Whisper, опционально разделение
  по спикерам через Deepgram). Файл или целая папка рекурсивно. Зови когда просят
  превратить аудио/видео в текст любыми словами: "расшифруй /путь", "транскрибируй
  видео", "сделай расшифровку папки", "прогони через транскрибатор", "распознай
  речь", "сделай стенограмму", "оцифруй запись", "вытащи текст из видео/аудио",
  "раздели по спикерам", "кто что сказал в записи" — даже без слова "транскрибация".
  Триггеры: расшифруй, расшифровка, расшифровать, расшифруйте, транскрибируй,
  транскрибация, транскрибатор, transcribe, стенограмма, распознай речь, текст
  из видео/аудио, спикеры, диаризация.
  НЕ вызывать: для перевода на другой язык (не переводчик); для сжатого
  пересказа/summary (выдаёт полный текст, не сокращает); для субтитров с
  таймкодами .srt (на выходе простой .md без таймингов).
---

# transcribe-batch — расшифровка аудио/видео

Работает одинаково в Claude Code и в Codex, на macOS, Linux и Windows.

## Где лежит скрипт

`transcribe.py` — рядом с этим файлом SKILL.md, в этой же папке.

## Что делает

- Один файл или папка (рекурсивно): `.mp4 .mov .mkv .webm .avi .m4v .mp3 .m4a .aac .wav .ogg .opus`
- Кладёт результат в `<имя файла>.md` рядом с исходником
- Уже расшифрованные файлы пропускает — safe перезапускать
- `--speakers` — разделение по спикерам через Deepgram (для диалогов/интервью)

## Шаг 0. Определи, чем запускать

Скрипту нужен только Python 3.8+ и ffmpeg. Никаких pip-пакетов ставить не надо.

- **macOS / Linux** — команда `python3`
- **Windows** — команда `python` (в PowerShell `python3` обычно открывает Microsoft Store)

Дальше в примерах написано `python3` — на Windows подставляй `python`.

## Шаг 1. Проверь установку одной командой

```
python3 "<путь_к_папке_скила>/transcribe.py" --check
```

Она печатает ОС, версию Python, найден ли ffmpeg/ffprobe, задан ли ключ, есть ли прокси.
Чего не хватает — подсказка по установке идёт в том же выводе. Не запускай расшифровку,
пока `--check` не скажет «Готово к работе».

Если нет ключа — скопируй `.env.example` в `.env` в папке скила и попроси пользователя
вставить бесплатный ключ с https://console.groq.com/keys. Для `--speakers` нужен ещё
`DEEPGRAM_API_KEY` с https://console.deepgram.com/

## Шаг 2. Запуск

Один файл:

```
python3 "<путь_к_папке_скила>/transcribe.py" "/абсолютный/путь/к/файлу.mp4"
```

Вся папка:

```
python3 "<путь_к_папке_скила>/transcribe.py" "/абсолютный/путь/к/папке"
```

Только план (что будет обработано, без запуска):

```
python3 "<путь_к_папке_скила>/transcribe.py" "/путь" --plan
```

С разделением по спикерам:

```
python3 "<путь_к_папке_скила>/transcribe.py" "/путь" --speakers
```

## Если Groq отдаёт HTTP 403 (не обслуживает страну — так в РФ)

**Настройки VPN, прокси, Clash и файрвола пользователя не трогай** — ни сам, ни просьбой
к нему. Это его рабочая инфраструктура, и для расшифровки её менять не нужно.

Правильное решение — второй движок:

```
python3 "<путь_к_папке_скила>/transcribe.py" "/путь" --deepgram
```

Deepgram работает оттуда, где Groq не работает, напрямую. Нужен `DEEPGRAM_API_KEY`
в `.env` — бесплатный ключ берётся на https://console.deepgram.com/

Если в `.env` лежат оба ключа, скрипт переключается сам: получив 403, он уходит на
Deepgram и пишет об этом строкой в выводе.

Прокси уместен ровно в одном случае: пользователь сам сказал, что хочет остаться на
Groq, и назвал порт своего уже настроенного прокси. Тогда — `HTTPS_PROXY=...` перед
командой в bash, либо `$env:HTTPS_PROXY = "..."` отдельной строкой в PowerShell.

## Прогресс для длинных прогонов

- macOS / Linux: `nohup ... > /tmp/transcribe.log 2>&1 &`, дальше `tail -f`.
- Windows: `nohup` нет. Либо просто не закрывать окно до конца прогона, либо
  `Start-Process` в отдельном окне. Предупреди пользователя, что окно должно жить.

## Отчёт

После запуска сообщи: сколько файлов найдено, какой движок (Groq/Deepgram).
После завершения: сколько готово, пропущено, ошибок, куда легли `.md`.
